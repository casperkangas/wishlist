//  Views/ContentView.swift
//  Placeholder — will be replaced with the full NavigationSplitView layout shortly.

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Wishlist loading...")
            .frame(width: 900, height: 620)
    }
}
